{
  "name": "@workers-demo/workers-demo",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server-model",
    "packages/server-workers",
    "packages/server-node",
    "packages/browser"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "publish": "npm run publish -ws --if-present"
  },
  "dependencies": {
    "tsconfig-paths": "^4.1.1"
  }
}
